let userInput = "     ";
let trimmedInput = userInput.trim();

if (trimmedInput.length > 0) {
  console.log("La entrada del usuario no está vacía");
} else {
  console.log("La entrada del usuario está vacía");
}
